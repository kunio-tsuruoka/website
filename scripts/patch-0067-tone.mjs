// issue-0067: 02-01〜03 の本文を だ/である調 → です/ます調 に統一。
// 機械置換は文法崩れの危険があるため、安全に確定できる語尾パターンのみ変換し、
// dry-run で変換後テキストを全出力して目視レビュー → --apply で本番。
//
// 使い方:
//   node --env-file=.env scripts/patch-0067-tone.mjs --slug vendor-decision-meeting          # dry-run（全文表示）
//   node --env-file=.env scripts/patch-0067-tone.mjs --slug vendor-decision-meeting --apply

import { createClient } from 'microcms-js-sdk';

const APPLY = process.argv.includes('--apply');
const si = process.argv.indexOf('--slug');
const SLUG = si >= 0 ? process.argv[si + 1] : null;

const client = createClient({
  serviceDomain: process.env.MICROCMS_SERVICE_DOMAIN,
  apiKey: process.env.MICROCMS_API_KEY,
});

// 安全に確定できる語尾変換（長いパターンを先に適用）。句読点・閉じ括弧の直前で適用。
// 「ない。」など形容詞で崩れるものは含めない（個別対応）。
const RULES = [
  [/であるべきだ。/g, 'であるべきです。'],
  [/のである。/g, 'のです。'],
  [/である。/g, 'です。'],
  [/だろう。/g, 'でしょう。'],
  [/になっている。/g, 'になっています。'],
  [/になった。/g, 'になりました。'],
  [/になる。/g, 'になります。'],
  [/が増える。/g, 'が増えます。'],
  [/していく。/g, 'していきます。'],
  [/している。/g, 'しています。'],
  [/された。/g, 'されました。'],
  [/される。/g, 'されます。'],
  [/だった。/g, 'でした。'],
  [/のだ。/g, 'のです。'],
  [/きやすい。/g, 'きやすいです。'],
  // suru動詞の文末（safe）
  [/する。/g, 'します。'],
  // 「〜ではない。」（safe）。動詞/形容詞の「ない。」は対象外（個別対応）
  [/ではない。/g, 'ではありません。'],
  // 名詞/na形容詞 + だ（上の長い だ系ルールの後に適用）
  [/だ。/g, 'です。'],
  // 個別（動詞/形容詞のない。等）
  [/届かない。/g, '届きません。'],
  [/にならない。/g, 'になりません。'],
  [/少ない。/g, '少ないです。'],
  // 「〜ていない。」「〜がない。」（safe）
  [/ていない。/g, 'ていません。'],
  [/がない。/g, 'がありません。'],
  // 個別動詞のない。
  [/動かない。/g, '動きません。'],
  [/言わない。/g, '言いません。'],
  [/取れない。/g, '取れません。'],
  [/分からない。/g, '分かりません。'],
  [/動けない。/g, '動けません。'],
  // 動詞・形容詞・体言止めの文末（すべて「。」直前アンカーで安全）
  [/ある。/g, 'あります。'],
  [/出る。/g, '出ます。'],
  [/遅れる。/g, '遅れます。'],
  [/測れる。/g, '測れます。'],
  [/受ける。/g, '受けます。'],
  [/見える。/g, '見えます。'],
  [/変える。/g, '変えます。'],
  [/答える。/g, '答えます。'],
  [/早まる。/g, '早まります。'],
  [/揉める。/g, '揉めます。'],
  [/来る。/g, '来ます。'],
  [/と言う。/g, 'と言います。'],
  [/捉える。/g, '捉えます。'],
  [/上がる。/g, '上がります。'],
  [/やる。/g, 'やります。'],
  [/伝える。/g, '伝えます。'],
  [/できる。/g, 'できます。'],
  [/渡す。/g, '渡します。'],
  [/映る。/g, '映ります。'],
  [/返す。/g, '返します。'],
  [/終わる。/g, '終わります。'],
  [/止まる。/g, '止まります。'],
  [/入れる。/g, '入れます。'],
  [/切り出す。/g, '切り出します。'],
  [/受け取った。/g, '受け取りました。'],
  [/受け取る。/g, '受け取ります。'],
  [/開いた。/g, '開きました。'],
  [/高い。/g, '高いです。'],
  [/多い。/g, '多いです。'],
  [/難しい。/g, '難しいです。'],
  [/やすい。/g, 'やすいです。'],
  [/抱え込みやすいです。/g, '抱え込みやすいです。'],
  [/ベスト。/g, 'ベストです。'],
  [/状態。/g, '状態です。'],
  [/準備は3つ。/g, '準備は3つです。'],
  // 2次バッチ（残検出ぶん）
  [/と断る。/g, 'と断ります。'],
  [/見えなくなる。/g, '見えなくなります。'],
  [/アクションを握る。/g, 'アクションを握ります。'],
  [/評価は高くなる。/g, '評価は高くなります。'],
  [/感じている。/g, '感じています。'],
  [/避けている。/g, '避けています。'],
  [/避けたい。/g, '避けたいです。'],
  [/リスクが大きい。/g, 'リスクが大きいです。'],
  [/ドラフトを開く。/g, 'ドラフトを開きます。'],
  [/案件は動き始める。/g, '案件は動き始めます。'],
  [/直接届ける。/g, '直接届けます。'],
  [/材料が直接届く。/g, '材料が直接届きます。'],
  [/理由を聞く。/g, '理由を聞きます。'],
  [/最初に決めておく。/g, '最初に決めておきます。'],
  [/機会を作る。/g, '機会を作ります。'],
  [/案件を進める。/g, '案件を進めます。'],
  [/ている。/g, 'ています。'],
  [/返し続ける。/g, '返し続けます。'],
  [/から動く。/g, 'から動きます。'],
];

function convert(html) {
  let out = html;
  for (const [re, to] of RULES) out = out.replace(re, to);
  return out;
}

(async () => {
  if (!SLUG) {
    console.error('--slug を指定してください');
    process.exit(1);
  }
  const cur = await client.get({
    endpoint: 'columns',
    contentId: SLUG,
    queries: { fields: 'content' },
  });
  const next = convert(cur.content);
  // 残存する だ/である系語尾（手動対応が必要なもの）を検出
  const text = next.replace(/<[^>]+>/g, ' ');
  const remaining = text.match(/[^。」)）]{0,12}(だ|である|する|した|ない|だろう)。/g) || [];

  console.log(`=== ${SLUG} ===`);
  console.log(`変換: ${cur.content !== next ? 'あり' : 'なし'}`);
  console.log(`\n--- 残存（だ/である系語尾・要手動確認） ${remaining.length}件 ---`);
  for (const r of remaining.slice(0, 40)) console.log(`  ${r.trim()}`);

  if (!APPLY) {
    console.log('\n[dry-run] --apply で本番適用');
    return;
  }
  await client.update({ endpoint: 'columns', contentId: SLUG, content: { content: next } });
  console.log('\n✅ PATCH 完了');
})().catch((e) => {
  console.error(e);
  process.exit(1);
});
