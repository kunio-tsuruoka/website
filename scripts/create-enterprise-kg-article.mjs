/**
 * 新規コラム作成: エンタープライズKGの設計パターンと構築プロセス。
 * 既定 dry-run（HTML出力＋AI-tellsスキャン）。--apply で MicroCMS へ作成（＝即公開）。
 */
import { createClient } from 'microcms-js-sdk';

const client = createClient({
  serviceDomain: process.env.MICROCMS_SERVICE_DOMAIN,
  apiKey: process.env.MICROCMS_API_KEY,
});

const SLUG = 'enterprise-knowledge-graph-design-patterns';
const CATEGORY = 'ai-development';
const APPLY = process.argv.includes('--apply');

const TITLE =
  'エンタープライズのナレッジグラフ設計パターン4種と構築プロセス｜1部門から始める実践手順';

const DESCRIPTION =
  '社内ドキュメント・業務プロセス・人材スキル・インシデントをつなぐエンタープライズのナレッジグラフ設計パターン4種と、1部門から始める構築7ステップを、各工程で具体的に何をやるかまで解説。大手企業の一部門で設計した経験も交えます。';

const CONTENT = `
<p>エンタープライズでナレッジグラフ（KG）を入れるとき、つまずくのは技術ではなく「どのパターンで、どこから、どう作るか」の設計です。全社の情報を一度につなごうとして頓挫する例が後を絶ちません。</p>
<p>この記事では、企業で使われる代表的な4つの設計パターンと、1部門から始める構築プロセスを、各ステップで具体的に何をやるかまで踏み込んで整理します。最後に、大手企業の一部門で実際にこの進め方でKGを設計した経験も、社名や具体的な業務を伏せて共有します。</p>
<h2>ナレッジグラフは何を「つなぐ」ものか</h2>
<p>ナレッジグラフは、社内の情報を「誰が・何に・どう関わるか」という関係でつないで持つ方法です。文書を単体で探すRAG（検索拡張生成）が苦手な、関係をたどる・正確に数える・「ないもの」を見つける・根拠を示す、という問いに答えられます。発注者目線の基礎は<a href="/column/knowledge-graph-rag-business">ナレッジグラフは発注者に何の得があるか</a>、技術面は<a href="/column/graphrag-knowledge-search">GraphRAGとは？ベクトルRAGとの違い</a>で扱っています。</p>
<h2>エンタープライズKGの4つの設計パターン</h2>
<p>企業のKGは、対象にする情報の種類で大きく4つに分かれます。自社のどの課題に効くかでパターンを選びます。</p>
<h3>パターン1：ドキュメントKG</h3>
<p>就業規則・規程・マニュアル・議事録・仕様書などの社内ドキュメントから、エンティティ（人・部署・製品・規程・用語）と関係を抽出してグラフにします。ドキュメント間の暗黙的な関連を明示化するのが狙いです。</p>
<p>効く問いは「この規程の承認者は誰か」「この用語はどの文書で定義されているか」。具体的には、見出しや章立てを構造として取り込み、固有名詞を用語辞書と突き合わせて名寄せし、文書から定義、改訂履歴へとたどれるチェーンを持たせます。版管理が肝で、古い版を最新と混同しないよう、文書ノードに有効期間を持たせておきます。</p>
<h3>パターン2：プロセスKG</h3>
<p>承認フロー・申請・依存タスクといった業務プロセスをグラフで表現します。ボトルネックと依存関係の可視化に効きます。</p>
<p>効く問いは「承認が止まっている申請はどれか」「この工程が遅れると何が止まるか」。タスクから前提タスク、担当ロール、承認条件をエッジで結び、イベントログ（誰がいつ何を承認したか）をグラフに流し込みます。承認の記録が結びついていない申請を「ないもの」として直接あぶり出せるのが強みです。フローの定義（版）と実際に流れた申請データは分けて持ちます。</p>
<h3>パターン3：人材・スキルKG</h3>
<p>社員のスキル・プロジェクト経験・専門知識をグラフにします。社員からスキル、案件、成果へとつなぎ、最適な人材配置と知識ギャップの発見に使います。</p>
<p>効く問いは「この技術を持っていて、いま手が空いている人は誰か」「この領域の知識が薄いのはどこか」。人事データ・プロジェクト履歴・成果物の著者情報からスキルを推定し、自己申告と突き合わせて確度を持たせます。スキル体系（オントロジー）を先に決めるのが成否を分けます。評価に直結する情報を扱うため、アクセス制御は厳格にします。</p>
<h3>パターン4：インシデントKG</h3>
<p>障害・インシデントの因果関係をグラフで管理します。事象から原因、対処、再発防止策、関連する設備やシステムへとつなぎ、根本原因分析の自動化と再発防止に使います。</p>
<p>効く問いは「同じ原因の障害は過去にあったか」「この部品が原因の事象は何件か」。ポストモーテムや対応チケットから事象・原因・対処を抽出して結び、類似インシデントをグラフの近傍から引き当てて一次対応に根拠つきで提示します。因果は人がレビューし、確定した因果と推定の段階を区別してエッジに持たせます。相関を因果と取り違えないためです。</p>
<h2>1部門から始める構築プロセス（7ステップ）</h2>
<p>全社一斉ではなく、効果の高い1部門・1プロセスから始めるのが鉄則です。各ステップで具体的に何をやるかを示します。</p>
<h3>ステップ1：スコープを限定する</h3>
<p>まず対象を1部門・1業務に絞ります。判断軸は、関係をたどることが価値を生む業務か、データが手に入るか、誤答したときのコストが見合うか。全社を一度に対象にするとオントロジーが発散し、どれも中途半端になります。最初の対象は、問い合わせが集中する規程・手順まわりか、属人化が痛い業務が向きます。</p>
<h3>ステップ2：既存データの棚卸し</h3>
<p>対象業務のデータがどこにあるかを洗い出します。Wiki、Slack、Jira、Confluence、ファイルサーバ、基幹システム。それぞれの形式（構造化か非構造化か）・更新頻度・アクセス権・機密区分を台帳にして、使える状態かを見極めます。個人情報や機密が含まれる場合は、マスキング方針とアクセス制御ポリシーをこの段階で決めます。</p>
<h3>ステップ3：オントロジー設計</h3>
<p>どのエンティティを、どの関係で結ぶかのスキーマ（オントロジー）を、現場のドメインエキスパートと一緒に定義します。ここが最も成否を分けます。最初から完璧を目指さず、対象業務の主要な問いに答えられる最小限のエンティティと関係から始め、抽出しながら育てます。用語のゆれ（同義語・略語）は用語辞書に集約しておきます。</p>
<h3>ステップ4：LLMによる自動抽出</h3>
<p>非構造化データ（文書・チャット・チケット）から、定義したオントロジーに沿ってエンティティと関係をLLMで抽出します。プロンプトにスキーマと抽出例を与え、出力をJSONで受け取ります。契約書のように文脈依存の意味解釈が要る対象は関係を先に抽出してからエンティティを特定し、カタログやマスタのように文脈に依存しない対象はエンティティを先に抽出して後から関係を付与する、と使い分けます。抽出結果には必ず出典（元文書と該当箇所）を残します。</p>
<h3>ステップ5：人間によるレビュー</h3>
<p>抽出結果は必ず人が検証します。とくに因果・権限・金額に関わる関係は、間違うと実害が出るため重点的に確認します。全件は見きれないので、確度スコアの低いものと重要度の高いものを優先します。レビューで見つかった誤りは、抽出プロンプトやオントロジーにフィードバックして精度を上げていきます。</p>
<h3>ステップ6：API・UI構築</h3>
<p>検索インタフェースとAPIを用意します。実務では、背景や手順の説明は文書検索（RAG）に、事実・数・根拠は関係（グラフ）に担当させる統合検索が効きます。オーケストレータが問いから検索計画を立て、全文検索・ベクトル検索・グラフ探索・RDBを束ねる検索ファサードが結果を統合する構成が一般的です。回答には必ず根拠（たどった関係の経路と出典）を添えます。</p>
<h3>ステップ7：継続的更新のパイプライン</h3>
<p>新しいデータを自動で取り込む仕組みを作ります。原本をオブジェクトストレージに置き、追加・更新のイベントをきっかけに各インデックス（全文・ベクトル・グラフ）へ逐次反映するイベント駆動の構成にすると、鮮度を保てます。規程やデータが変わっても関係の側を更新すれば、回答がその場で最新に切り替わります。あわせて、孤立ノードや重複エンティティを検出する品質監視も回します。</p>
<h2>参考：統合検索のアーキテクチャ像</h2>
<p>ステップ6と7を具体化すると、次のような構成になります。特定の製品に依存しない一般的な形です。</p>
<ul>
<li>オーケストレータ：問いを解釈し、どの検索をどう組み合わせるか計画する</li>
<li>検索ファサード：全文検索エンジン、ベクトルインデックス、グラフDB、RDBを束ねて統合検索する</li>
<li>原本ストレージ：文書の原本をオブジェクトストレージに保持する</li>
<li>イベント駆動更新：原本の追加・更新をメッセージバスで各インデックスに逐次反映する（GraphRAGのグラフも同時に更新）</li>
</ul>
<p>クラウドのマネージド検索とグラフDBを組み合わせる作り方もあれば、VPS上に全文・ベクトル・グラフを載せて安く始める作り方もあります。規模と可用性の要件で選びます。この線引きは<a href="/column/genai-system-infrastructure">生成AIシステムのインフラはVPSで十分か</a>で整理しています。</p>
<h2>事例：大手企業の一部門で、こう進めた</h2>
<p>私たちは、大手企業の一部門を対象に、社内の暗黙知（口伝や属人的な業務手順）を可視化する目的でナレッジグラフを設計した経験があります。守秘のため社名や具体的な業務は伏せますが、進め方はこの記事の7ステップに沿っています。</p>
<p>最初から全社を対象にせず、1部門・特定の業務にスコープを絞りました。対象業務のドキュメントと業務プロセスからエンティティと関係を洗い出し、オントロジーを現場のドメイン担当者と一緒に定義。非構造化データからLLMで抽出し、確度の低い関係と重要度の高い関係を人手でレビューして精度を上げました。</p>
<p>狙いは、まず1つの業務を「AIが根拠つきで答えられる」状態にして動かし、そこで固めた設計を横へ広げること。全社一斉の大規模構築を避け、効果を確認しながら対象を広げる方針で進めました。</p>
<h2>やりすぎない判断</h2>
<p>4つのパターンをすべて一度に作る必要はありません。背景や手順を聞きたいだけの業務は、文書検索（RAG）のほうが安く早く済みます。関係をたどる・数える・抜けを探す・根拠を示す、が自社の問いに含まれるときに、ナレッジグラフを足す価値が出ます。まず1部門・1業務で効果を確かめ、そこから広げるのが堅実です。回答精度そのものの底上げは<a href="/column/ai-rag-accuracy-graphrag">生成AIの回答精度を業務レベルに引き上げる方法</a>で扱っています。</p>
<h2>よくある質問（FAQ）</h2>
<h2>Q. 4つのパターンのどれから始めるべきですか？</h2>
<p>A. 効果が見えやすく、データが手に入る業務から選びます。問い合わせが集中する規程・手順まわりならドキュメントKG、承認や申請の停滞が痛いならプロセスKG、人の配置に困っているなら人材・スキルKG、障害対応の属人化が課題ならインシデントKGです。まず1つに絞ります。自社の問いがKG向きかは<a href="/column/knowledge-graph-rag-business">ナレッジグラフは発注者に何の得があるか</a>で確認できます。</p>
<h2>Q. オントロジー設計は何から手をつければよいですか？</h2>
<p>A. 対象業務で「答えたい問い」を10個ほど書き出し、その問いに答えるのに必要なエンティティと関係だけを最小限で定義します。最初から網羅しようとせず、抽出しながら育てます。用語のゆれは辞書に集約し、現場のドメイン担当者との共同作業として進めます。</p>
<h2>Q. LLMの抽出結果はどこまで信用できますか？</h2>
<p>A. そのままでは業務に使えません。とくに因果・権限・金額に関わる関係は人のレビューが必須です。抽出には必ず出典を残し、確度スコアの低いものから確認します。精度を上げる考え方は<a href="/column/ai-rag-accuracy-graphrag">生成AIの回答精度を業務レベルに引き上げる方法</a>で詳しく扱っています。</p>
<h2>Q. マネージドサービスとVPS自前、どちらで作るべきですか？</h2>
<p>A. 規模と可用性の要件で決めます。運用の手間を減らしたい、高い可用性が要るならマネージド、安く小さく始めるならVPS自前でも十分に作れます。判断軸は<a href="/column/genai-system-infrastructure">生成AIシステムのインフラはVPSで十分か</a>で整理しています。</p>
<h2>Q. 社内AIアシスタントにナレッジグラフを組み込むと何が変わりますか？</h2>
<p>A. 「それらしいが使えない」で止まりがちな社内AIが、根拠つきで正確に答えられるようになります。成功と失敗の分かれ目は<a href="/column/internal-ai-assistant-rag-patterns">社内AIアシスタント導入の成功と失敗パターン</a>で整理しています。</p>
<h2>Beekleの取り組み</h2>
<p>Beekleは、ナレッジグラフの設計を「どのパターンで、どこから、どう作るか」から一緒に決めます。全社を一度につなぐ大規模構築ではなく、効果の高い1部門・1業務にスコープを絞り、オントロジー設計からLLM抽出、レビュー、統合検索、継続的な更新まで一貫して手がけます。RAGとグラフの組み合わせ方は<a href="/services/rag-system-development">RAGシステム構築</a>で、まず小さく試したい方は初期費用0円の<a href="/prooffirst">ゼロスタート</a>からご相談ください。</p>
<p>{{CONTACT_CTA}}</p>
`.trim();

// --- AI-tells / markdown 残骸スキャン ---
const scan = {
  'em dash ——': /——/,
  '全角スラッシュ／': /／/,
  二重引用符: /[“”]/,
  'markdown bold **': /\*\*/,
  'markdown heading #': /(^|\n)#{1,4}\s/,
  冗長主語: /自[分身]が(思う|考える|感じる|抱く|描く)/,
  過剰強調定型句: /に他なりません|過言ではありません/,
  使い古し比喩: /地図|羅針盤|土台|両輪|潤滑油|レシピ|スパイス|栄養|筋トレ|DNA/,
};
console.log('=== AI-tells scan (content+description) ===');
const blob = CONTENT + DESCRIPTION;
let hit = false;
for (const [k, re] of Object.entries(scan)) {
  const m = blob.match(re);
  if (m) {
    hit = true;
    console.log(`  HIT ${k}: ${JSON.stringify(m[0])}`);
  }
}
console.log(hit ? '  ^ 要修正' : '   all clean');
console.log('strong count:', (CONTENT.match(/<strong>/g) || []).length);
console.log('FAQ Q (h2 Q.) count:', (CONTENT.match(/<h2>Q\./g) || []).length);
console.log(
  'internal links:',
  (CONTENT.match(/href="\/(column|services|prooffirst)/g) || []).length
);
console.log('title:', TITLE);
console.log('slug:', SLUG, '| category:', CATEGORY);
console.log('content bytes:', CONTENT.length);

if (!APPLY) {
  console.log('\n[dry-run] --apply で MicroCMS に作成（即公開）します。');
  process.exit(0);
}

// 既存チェック
try {
  await client.get({ endpoint: 'columns', contentId: SLUG, queries: { fields: 'id' } });
  console.error(`\n既に ${SLUG} が存在します。中止。`);
  process.exit(1);
} catch (_e) {
  /* 404 = 未作成、続行 */
}

await client.create({
  endpoint: 'columns',
  contentId: SLUG,
  content: { title: TITLE, content: CONTENT, category: CATEGORY, description: DESCRIPTION },
});

const fetched = await client.get({
  endpoint: 'columns',
  contentId: SLUG,
  queries: { fields: 'id,title,content,category' },
});
const faqQ = (fetched.content.match(/<h2[^>]*>Q\./g) || []).length;
const ok =
  fetched.content.includes('パターン1：ドキュメントKG') &&
  fetched.content.includes('事例：大手企業の一部門で、こう進めた') &&
  fetched.content.includes('{{CONTACT_CTA}}') &&
  faqQ === 5;
console.log(`\n[apply] 作成完了: ${fetched.id} / cat=${fetched.category?.id}`);
console.log(
  `FAQ Q count(stored): ${faqQ} / links stored: ${(fetched.content.match(/href="\//g) || []).length}`
);
console.log(`検証: ${ok ? 'OK' : 'NG(要確認)'} — 本番 /column/${SLUG}`);
