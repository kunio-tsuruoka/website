---
import { CTASection } from '@/components/process-cta-section';
import { ProcessFAQ } from '@/components/process-faq-section';
import { ProcessHero } from '@/components/process-hero';
import { ProcessSteps } from '@/components/process-steps';
import { Header } from '../components/header';
import { ServiceCard } from '../components/service-card';
import { services } from '../data/service';
import Layout from '../layouts/layout.astro';
import { HeroSection } from '@/components/hero-section';
import ContactForm from '@/components/contact-form';
---

<Layout title="Beekle - 技術を用いて人を幸せに">
  <Header client:load />
  <main class="">
    {/* メインビジュアル */}
    <HeroSection client:load />

    {/* サービス一覧 */}
    <section id="services" class="relative py-32 overflow-hidden">
      {/* 背景装飾 */}
      <div class="absolute inset-0">
        <div class="absolute top-0 left-0 w-96 h-96 bg-purple-100/20 rounded-full blur-3xl" />
        <div class="absolute bottom-0 right-0 w-96 h-96 bg-indigo-100/20 rounded-full blur-3xl" />
      </div>
      
      <div class="container mx-auto px-8 lg:px-12 relative">
        <div class="text-center mb-16">
          <h2 class="text-5xl lg:text-6xl font-bold text-gray-900 mb-6">
            Our <span class="text-transparent bg-clip-text bg-gradient-to-r from-indigo-600 to-purple-600">Services</span>
          </h2>
          <p class="text-xl text-gray-600 max-w-3xl mx-auto">
            お客様のビジネスに寄り添ったマーケティング・情報設計・デザイン・開発等のトータルサポートを行います
          </p>
        </div>
        
        <div class="grid md:grid-cols-3 gap-8">
          {services.map((service, index) => (
            <div
              class="transform transition-all duration-500 hover:-translate-y-2"
              style={{ transitionDelay: `${index * 100}ms` }}
            >
              <ServiceCard
                client:visible
                id={service.id}
                title={service.title}
                description={service.description}
              />
            </div>
          ))}
        </div>
      </div>
    </section>

    {/* 開発の流れ */}
    <section id="process" class="relative bg-gradient-to-br from-gray-50 to-white py-32">
      <div class="absolute inset-0 bg-grid-pattern opacity-5" />
      <div class="relative">
        <ProcessHero client:load />
        <div class="container mx-auto">
          <ProcessSteps client:load />
        </div>
      </div>
    </section>


    {/* よくあるお悩み */}
    <section class="py-32 bg-gradient-to-br from-indigo-50 via-white to-purple-50">
      <div class="container mx-auto px-8 lg:px-12">
        <div class="text-center mb-16">
          <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-4">
            よくある<span class="text-transparent bg-clip-text bg-gradient-to-r from-indigo-600 to-purple-600">お悩み</span>
          </h2>
          <p class="text-xl text-gray-600">お客様からよくいただくご相談をまとめました</p>
        </div>
        
        <div class="grid md:grid-cols-2 gap-6 max-w-5xl mx-auto">
          <div class="group relative">
            <div class="absolute inset-0 bg-gradient-to-r from-indigo-600 to-purple-600 rounded-2xl blur opacity-20 group-hover:opacity-30 transition-opacity" />
            <div class="relative bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-all">
              <div class="flex items-start">
                <span class="text-4xl text-indigo-600 mr-4">Q</span>
                <div>
                  <h4 class="text-xl font-bold text-gray-800 mb-3">
                    システム開発の予算が見えづらい
                  </h4>
                  <p class="text-gray-600 leading-relaxed">
                    要件定義フェーズでの綿密な計画立案と、アジャイル開発による段階的な開発により、予算の透明性を確保します。
                  </p>
                </div>
              </div>
            </div>
          </div>
          
          <div class="group relative">
            <div class="absolute inset-0 bg-gradient-to-r from-purple-600 to-pink-600 rounded-2xl blur opacity-20 group-hover:opacity-30 transition-opacity" />
            <div class="relative bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-all">
              <div class="flex items-start">
                <span class="text-4xl text-purple-600 mr-4">Q</span>
                <div>
                  <h4 class="text-xl font-bold text-gray-800 mb-3">
                    最適な技術スタックが分からない
                  </h4>
                  <p class="text-gray-600 leading-relaxed">
                    プロジェクトの規模、予算、期間、将来の拡張性を考慮し、実績のある技術スタックを提案いたします。
                  </p>
                </div>
              </div>
            </div>
          </div>

          <div class="group relative">
            <div class="absolute inset-0 bg-gradient-to-r from-pink-600 to-indigo-600 rounded-2xl blur opacity-20 group-hover:opacity-30 transition-opacity" />
            <div class="relative bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-all">
              <div class="flex items-start">
                <span class="text-4xl text-pink-600 mr-4">Q</span>
                <div>
                  <h4 class="text-xl font-bold text-gray-800 mb-3">
                    システムの安定性に不安がある
                  </h4>
                  <p class="text-gray-600 leading-relaxed">
                    24時間365日のモニタリング体制と、自動化された障害検知システムにより、問題の早期発見・対応が可能です。
                  </p>
                </div>
              </div>
            </div>
          </div>

          <div class="group relative">
            <div class="absolute inset-0 bg-gradient-to-r from-indigo-600 to-purple-600 rounded-2xl blur opacity-20 group-hover:opacity-30 transition-opacity" />
            <div class="relative bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-all">
              <div class="flex items-start">
                <span class="text-4xl text-indigo-600 mr-4">Q</span>
                <div>
                  <h4 class="text-xl font-bold text-gray-800 mb-3">
                    保守・運用コストが高騰している
                  </h4>
                  <p class="text-gray-600 leading-relaxed">
                    自動化ツールの導入や運用プロセスの最適化により、保守コストを削減。クラウドの特性を活かした柔軟なリソース管理を実現します。
                  </p>
                </div>
              </div>
            </div>
          </div>
        </div>
        
        <div class="text-center mt-12">
          <a href="/development-issues" class="inline-flex items-center text-indigo-600 hover:text-indigo-700 font-medium text-lg group">
            開発問題の事例を詳しく見る
            <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      </div>
    </section>

    {/* よくある質問 */}
    <section id="faq" class="py-32 bg-white">
      <ProcessFAQ client:load />
    </section>

    {/* お問い合わせフォーム */}
    <section class="py-32 bg-gradient-to-br from-gray-50 to-white relative overflow-hidden">
      {/* 背景装飾 */}
      <div class="absolute inset-0">
        <div class="absolute top-0 right-0 w-96 h-96 bg-indigo-100/30 rounded-full blur-3xl" />
        <div class="absolute bottom-0 left-0 w-96 h-96 bg-purple-100/30 rounded-full blur-3xl" />
      </div>
      
      <div class="container mx-auto px-8 lg:px-12 relative">
        <div class="text-center mb-12">
          <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-4">
            お気軽に<span class="text-transparent bg-clip-text bg-gradient-to-r from-indigo-600 to-purple-600">ご相談</span>ください
          </h2>
          <p class="text-xl text-gray-600">まずは無料相談から始めましょう</p>
        </div>
        <div class="max-w-2xl mx-auto">
          <ContactForm />
        </div>
      </div>
    </section>

    {/* CTA Section */}
    <section class="relative py-32 overflow-hidden">
      <div class="absolute inset-0 bg-gradient-to-br from-indigo-600 to-purple-600" />
      <div class="absolute inset-0 bg-grid-pattern opacity-10" />
      
      <div class="container mx-auto px-8 lg:px-12 relative">
        <div class="text-center">
          <h2 class="text-4xl lg:text-5xl font-bold text-white mb-6">
            ビジネスの成長を加速させましょう
          </h2>
          <p class="text-xl text-white/90 mb-10 max-w-2xl mx-auto">
            技術とクリエイティブの力で、お客様のビジネスに新たな価値を創造します
          </p>
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a
              href="/contact"
              class="inline-flex items-center justify-center px-8 py-4 text-lg font-medium text-indigo-600 bg-white rounded-full hover:bg-gray-100 transition-all shadow-lg hover:shadow-xl"
            >
              無料相談を予約する
              <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
              </svg>
            </a>
            <a
              href="/case-studies"
              class="inline-flex items-center justify-center px-8 py-4 text-lg font-medium text-white bg-transparent border-2 border-white rounded-full hover:bg-white hover:text-indigo-600 transition-all"
            >
              導入事例を見る
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0) }
    50% { transform: translateY(-20px) }
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }

  .animate-float-delay {
    animation: float 8s ease-in-out infinite;
    animation-delay: 1s;
  }

  .animate-float-slow {
    animation: float 10s ease-in-out infinite;
    animation-delay: 2s;
  }

  .bg-grid-pattern {
    background-image: linear-gradient(rgba(255, 255, 255, 0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.1) 1px, transparent 1px);
    background-size: 20px 20px;
  }
</style>